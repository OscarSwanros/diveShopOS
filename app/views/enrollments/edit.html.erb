<div class="max-w-2xl">
  <h1 class="text-2xl font-bold mb-6"><%= t("enrollments.edit.title") %></h1>
  <p class="text-sm text-gray-500 mb-4"><%= @course.name %> &middot; <%= @enrollment.customer.full_name %></p>
  <%= render "form", enrollment: @enrollment, course: @course, course_offering: @course_offering, customers: @customers %>
</div>
