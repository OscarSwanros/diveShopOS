<div class="space-y-6">
  <h1 class="text-2xl font-bold"><%= t("service_records.edit.title") %></h1>
  <p class="text-sm text-gray-500"><%= @equipment_item.name %></p>
  <%= render "form", service_record: @service_record %>
</div>
