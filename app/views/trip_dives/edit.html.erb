<div class="max-w-2xl">
  <h1 class="text-2xl font-bold mb-6"><%= t("trip_dives.edit.title") %></h1>
  <%= render "form", excursion: @excursion, trip_dive: @trip_dive, dive_sites: @dive_sites %>
</div>
